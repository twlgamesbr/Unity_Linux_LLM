---
description: Snapshot Docker infrastructure state into long-term memory
---

Run `bash .opencode/scripts/infra-snapshot.sh` and report the current status of every container.

Show a summary table with container name, image, ports, and uptime. Include the Qdrant, LocalAI, PostgreSQL, and NPC server status.
